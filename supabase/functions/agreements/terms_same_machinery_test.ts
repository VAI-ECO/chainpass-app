Deno.test("first-visit terms use subtype terms on same agreements tables", () => {
  const subtype = "terms";
  const type = "single";
  if (subtype !== "terms" || type !== "single") throw new Error("same machinery");
});
